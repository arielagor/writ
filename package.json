{
  "name": "writ-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Writ: the delegation model an enterprise writes before its agents run, and the proof that it holds. Purpose manifests compiled to Cedar policy and Claude Code permissions.",
  "license": "MIT",
  "author": "Ariel Agor",
  "type": "module",
  "bin": {
    "writ": "dist/bin/writ.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/arielagor/writ.git"
  },
  "keywords": [
    "ai-agents",
    "authorization",
    "cedar",
    "purpose-limitation",
    "audit-log",
    "mcp",
    "claude-code"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test \"test/**/*.test.ts\"",
    "build": "tsc -p tsconfig.build.json",
    "writ": "tsx bin/writ.ts",
    "verify": "tsx bin/writ.ts verify",
    "probe": "tsx bin/writ.ts probe",
    "hook:selftest": "node hooks/writ-gate.mjs --self-test --manifest examples/gbrain-reader.yaml",
    "golden:update": "tsx bin/writ.ts compile examples/blog-publisher.yaml --target all --out test/golden/blog-publisher && tsx bin/writ.ts compile examples/gbrain-reader.yaml --target all --out test/golden/gbrain-reader"
  },
  "dependencies": {
    "@cedar-policy/cedar-wasm": "^4.12.0",
    "ajv": "^8.20.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
